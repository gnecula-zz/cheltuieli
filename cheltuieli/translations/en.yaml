configuration:
  admin_users:
    name: HA admins
    description: Home Assistant usernames or user IDs, comma-separated. Empty = first visitor becomes admin.
  ai_api_key:
    name: API key
    description: Optional. Choose provider and model in the app Settings; they persist across updates.
network: {}
